'use strict'

import React from 'react'

function SeccionesLayout(props) {
    return(
        <div className="SeccionesLayout">
            {props.children}
        </div>
    )
}

export default SeccionesLayout;